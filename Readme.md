# Juhi Wedding Website

A static wedding invitation website built with HTML, CSS, and JavaScript.

## Run locally

Open `index.html` in a browser, or serve this folder with any static web server.

## GitHub Pages

This project is ready to publish from the repository's `main` branch using GitHub Pages.
